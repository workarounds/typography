apply plugin: 'java'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile project(':typography-annotations')

    compile deps.autoservice
    compile deps.autocommon
    compile deps.javapoet
}